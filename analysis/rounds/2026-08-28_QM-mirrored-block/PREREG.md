# 預註冊：工單 Q 與 M 的鏡像區塊 `base Q M M Q base`

**2026-08-28，`8/28 mainDev` 寫，第一臂開跑之前。**
本檔註冊**臂的排列本身**——排列是實驗設計的一部分，先前它只以一行字存在於 `NEXT.md`。

---

## 0. 這個區塊要交付什麼

9/03 投影片上兩張圖現在是空的（M 那張逐字寫著 NO ARM HAS RUN YET）。

| 工單 | 圖要回答的 | 比較 |
|---|---|---|
| **Q** | 硬編碼 1000 ms 分母造成多少高報，修掉之後還剩多少 | `3367d0e9` ↔ `ab2d7ed1` |
| **M** | path 重算 1 kHz → 1 Hz 讓一條新流多久沒有路徑 | `ab2d7ed1` ↔ `a40e04ce` |

`ab2d7ed1` 是**共用樞紐**：兩個比較各只差一個變數。

---

## 1. 排列：`base Q M M Q base`（審查員給的設計，理由寫在這裡）

| 位置 | 1 | 2 | 3 | 4 | 5 | 6 | 平均位置 |
|---|---|---|---|---|---|---|---|
| **base** `3367d0e9` | ● | | | | | ● | **3.5** |
| **Q** `ab2d7ed1` | | ● | | | ● | | **3.5** |
| **M** `a40e04ce` | | | ● | ● | | | **3.5** |

**三個條件的平均位置相同 ⇒ 只要漂移在這段時間內近似線性，它在每一個兩兩差值裡完全抵消**，
而且**不需要知道漂移的大小或機制**。

對照——輪轉 `A B C A B C` 的平均位置是 2.5 / 3.5 / 4.5，
**每個兩兩比較都固定被 1×d 汙染**，d 未知且不可估。

### 1-bis. 這個排列額外免費給的東西：線性檢定（**診斷，不是門檻**）

三個條件各自的**組內**差值，位置間距分別是 5、3、1：

| 條件 | 位置 | 間距 | 線性漂移下的期望差值 |
|---|---|---|---|
| base | 1, 6 | **5** | 5·d |
| Q | 2, 5 | **3** | 3·d |
| M | 3, 4 | **1** | 1·d |

⇒ 若漂移以線性為主，三個組內差值應約略呈 **5 : 3 : 1**，且同號。

🔴 **這是診斷不是門檻，理由要先寫**：每個差值各自帶約 ±43 ms 的臂間噪聲（見 §2），
所以就算真有 10 ms/位置 的斜率（5d = 50 ms），也只是勉強可見。
**一個區塊的資料不足以判定線性；不得用它「確認設計成立」。** 它只在**明顯違反**時有用
（例如 M 的組內差值反而最大）——那才是「漂移不是線性主導」的訊號。

---

## 2. 🔴 先解決 42 ms 的矛盾（審查員要求：查原始讀數，不要猜）

兩份 log 對同一個「42 ms」講了不同的話：

| 檔案 | 說法 |
|---|---|
| `survey_T64.log:13,23,33` | 「moved 42 ms **between two fabric generations**」 |
| `drive_Q.log:29,62` | 「two quiet arms of the **SAME generation** came out 42 ms apart」 |

### 查到的：兩者都真的被寫過，而且**是兩個不同的 42**

`measure_loop_period.py` 的頁尾文字在兩次跑之間被改過：

- `5695ce8`（08-27 17:15）寫入「between two fabric generations」→ `survey_T64.log` 17:34 印的是它
- `60dcca6`（08-27 17:42）改成「SAME generation」→ `drive_Q.log` 22:32 印的是它

⇒ **不是兩個並存的信念，是一份 log 帶著已被撤回的舊字串。**

### 原始讀數（四個安靜臂，全部實測）

| 世代 | 安靜臂 | `T` (ms) |
|---|---|---|
| gen 1 | `Q` | **1043.9** ± 3.4 |
| gen 1 | `Q′` | **1016.7** |
| gen 2 | `P_Q` | **1001.7** ± 5.8 |
| gen 2 | `P_Qp` | **1043.3** ± 20.1 |

- **42.2 ms ＝ 1043.9 − 1001.7 ＝ 跨代**，但那是**各取一個臂**，世代與臂完全混淆。**已撤回**（`60dcca6`）。
- **41.6 ms ＝ 1043.3 − 1001.7 ＝ gen 2 的兩個安靜臂**，**同代、臂間**。
- gen 1 自己的兩個安靜臂差 **27.2 ms**，也是同代臂間。
- 以臂為複製單位重算：gen 1 均值 1030.3、gen 2 均值 1022.5，**Δ = +7.8 ms、臂間 SE 24.9、Δ/SE = 0.31** ⇒ **跨代差異不成立**。

### 🔑 結論（可用來設門檻）

> **同代兩個安靜臂的 `T` 差值，已被量到兩次：27.2 ms 與 41.6 ms。**

由這兩個差值反推（常態下 E|X−Y| = 1.128σ）：**臂間 σ ≈ 30 ms，差值的 SD ≈ 43 ms。**
⚠️ **n = 2 個差值**，這是弱估計，本區塊的 base 對會是第三個。

**順手修掉的**：`measure_loop_period.py` 的 **docstring**（:10-16）到今天為止**仍寫著**已撤回的
跨代宣稱，並據此下結論「period 是世代與負載的性質」。`60dcca6` 只改了 `print()`，漏了 docstring。
已改（本輪 commit），寫回「period 是**臂**的性質」。

---

## 3. 🔴 漂移門檻（先寫數字，再看資料）

**兩個 base 臂（位置 1、6）就是漂移計。**

### 3-1. `T` 的作廢門檻

| 判準 | 數字 | 依據 |
|---|---|---|
| `\|T(base1) − T(base6)\|` ≤ **90 ms** | 通過 | 同代已觀察到 27.2、41.6；差值 SD ≈ 43 ms ⇒ 90 ms ≈ 2σ_diff |
| > **90 ms** | 🔴 **本區塊的 `T` 相關讀數作廢** | 這是這座 fabric 從未出現過的區間 |

### 3-2. `ratio` 的作廢門檻

⚠️ **ratio 的臂間離散度從來沒有被量過**（`T` 有，ratio 沒有）。**不得回填一個看起來合理的數字。**
⇒ 改用**相對於效應**的判準，這不需要先驗離散度：

| 判準 | 動作 |
|---|---|
| `\|ratio(base1) − ratio(base6)\|` **< 一半的 base↔Q 差值** | Q 的讀數成立 |
| ≥ **一半** | 🔴 **Q 的 ratio 讀數作廢**（漂移計比訊號的一半還大） |

**這兩個 base 臂同時是 ratio 臂間離散度的第一次測量**，數值本身要寫進報告，不論通過與否。

### 3-3. 🔴 作廢之後做什麼（留白等於沒有判準）

1. **先不換工作點、不加臂數**：重跑**同一個區塊一次**，新的 fabric 世代。
   理由：漂移是時間的函數，換世代是唯一乾淨的重抽樣；加臂只會把更長的時間收進同一個區塊，讓漂移更大。
2. 若**第二個區塊也作廢** ⇒ 停止跑臂，改成「這座測試床在 6 臂 × 40 分鐘的尺度上不穩定」這個**獨立發現**去報，
   並改用**臂內配對**設計（同一臂內交替 binary）——那要改 kernel 的熱替換能力，是另一張工單。
3. **不得**在看過資料之後才決定丟哪一個臂。

---

## 4. 🔴 Q 的功率前提（**在 base1 跑完就可判定，但判準現在就寫死**）

Q-quater 已經指出過這個坑：Q 的 ratio 效應大小 **＝ (T − 1000 ms) / 1000 ms**。
- gen 1 的 T = 1043.9 ⇒ 高報 **+4.4%**，量得到。
- gen 2 的 T = 1001.7 ⇒ 高報 **+0.2%**，**落在噪聲裡，Q 什麼都量不到**。

**先寫死的判讀規則：**

| base 臂的 `T` | Q 的讀數怎麼標 |
|---|---|
| 平均 ≥ **1020 ms** | Q 的 ratio 比較**有力**，照 §5 判讀 |
| 平均在 **1000–1020 ms** | 🔴 **INCONCLUSIVE-BY-DESIGN**：效應 < 2%，本區塊解析不到。**不得寫成「Q 沒有效果」** |
| 平均 < **1000 ms** | 🔴 前提本身被推翻（迴圈比 1 s 還快）⇒ 獨立發現，回頭讀 `sleep_for` 那段 |

🔑 **不因為 base1 的讀數而改變區塊**（那是適應性設計）。**區塊照跑**——M 仍然需要它——
只是 Q 的那格照上表貼標籤。

---

## 5. 預註冊區間（**區間不是方向**）

### 5-Q. 工單 Q

**主要量先指名**：`analyze.py` 給三個邊類別各一個 ratio，而**同一個 T 下三類的 ratio 本來就不同**
（①實測 host→switch 1.0373、switch→host 1.0671、switch→switch 1.0412）。
⇒ **主要量 ＝ 逐類別取 base − Q 的差，再平均**。類別自身的偏差在差值裡消掉，
分母效應不會——它對三類是共同的。三類各自的數字仍然全報。

| 量 | base `3367d0e9` | Q `ab2d7ed1` | 落在區間外怎麼辦 |
|---|---|---|---|
| `ratio`（分身報的速率 ÷ veth 實際） | **1.00 + (T−1)** ± 0.02 | **1.00** ± 0.02 | base 若 ≈ 1.00 ⇒ 高報來自別處，回頭找 |
| **逐類別差值的平均**（主要量） | — | — | 預測 ≈ **T − 1**；差一個數量級 ⇒ 分母不是唯一機制 |
| `T`（迴圈週期） | 兩者**應相同** | 兩者**應相同** | 若 Q 的 T 系統性不同 ⇒ 修改本身改了迴圈時間，是新發現 |
| 分母自證行 | **不存在**（記 ABSENT，不是 PASS） | `\|measured−divisor\|/measured` ≤ **1%** | > 1% ⇒ 修沒生效 |

### 5-M. 工單 M（照 M-quater Mq-3，這裡只補 `never` 的處置）

| 臂 | `t_first_path − t_first_seen` 的預測 |
|---|---|
| **Q `ab2d7ed1`**（1 kHz） | **< 一個輪詢週期**（≈ 0.1 s，量不到） |
| **M `a40e04ce`**（1 Hz） | 平均 **0.4–0.6 s**、**p95 < 1.0 s**、`never` 比例 ≈ 0 |

**區間外先寫死：**
- **M 臂量到 ≈ 0** ⇒ 🔴 path 不是只在 path pass 被寫入，回頭找第二個寫入點
- **M 臂 > 1.0 s** ⇒ 🔴 重算迴圈的實際週期不是 1 s（`sleep_for` 不是 `sleep_until`，**與 Q 同形狀**）
- **以上皆非** ⇒ 記成獨立結果，**不塞進最近的分支**

**`never` 的處置（不得混）**：
- `never_path=true` **且** `watch_s ≥ 3 s` ⇒ 真的沒拿到路徑，計入 `never` 比例
- `never_path=true` **但** `watch_s < 3 s` ⇒ **右設限**，從分母移除並**單獨報數量**
- 第一次成功輪詢就在表裡的流 ⇒ **左設限**，偵測延遲只算下界，**排除在 M 的效應之外**

### 5-D. 偵測延遲（`t_first_seen` − 流開始送封包）——**本專案從未量過**

不是 M 的效應，但同一份資料免費產生。churn schedule 有每條流的預定啟動秒，
所以 `t_first_seen − at` 可算。**只報分布，不設區間**（沒有先驗，設了就是編的）。

---

## 6. 工作點：六臂**完全相同**

| 項目 | 值 |
|---|---|
| 穩定流 | `FLOW_LIMIT=16`、`FLOW_S=240`、`WARM_S=20` ⇒ 量測窗 200 s |
| churn | `TOTAL_S=180`、`EVERY_S=5`、`BATCH=4`、`SEED=20260828` ⇒ **144 條、1–4 s** |
| path 取樣 | **10 Hz**、200 s |
| burners | **0** |
| `T` reps | **1**（窗長 200 s） |
| 臂間靜置 | 20 s |
| fabric 世代 | **整個區塊共用一個**，不重建 |

🔴 **穩定流與 churn 同時跑，六臂皆然。** 這是**新的組合**（先前 Q 只跑穩定流、M 只跑 churn），
⇒ **先跑一個縮時煙霧臂**，同一條程式路徑，不是另一條。

🔴 **輪詢擾動的基線**（M-quater (ii-i) 要求，不做則整組數字不可用）：
區塊開跑前先跑一次**只輪詢、不放流量**的 60 s，記錄達成的輪詢間隔與 API 回應時間；
**每一臂的 `poll.jsonl` 也逐次記 `ms`**，所以擾動的證據在臂自己身上，不只在基線裡。

---

## 7. 順手保留、**不改實驗**的東西

臂本來就在跑 iperf3。**保留 per-interval 輸出**（Adam 要一張「數十 GB 流量下 jitter」的圖）。

🔴 **這不是加一個測量，是不要把已經有的資料丟掉。**
**若保留它需要改任何臂的參數、時長或工作點，就不保留。** 實作上只是不丟棄 iperf3 的 JSON 區間段。

---

## 8. 本區塊**不**宣稱

- **不**宣稱漂移是線性的。§1-bis 是診斷，一個區塊測不動它。
- **不**宣稱跨世代可比。§2 已經示範這個比較怎麼出錯的。
- **不**宣稱 `never` 比例在右設限流被排除後仍然無偏——右設限的**判準**（3 s）本身是我訂的。
- **不**宣稱進表門檻是什麼（M-quater Mq-4 已列，仍未查）。**若它與取樣率相關，M 的延遲在不同取樣率下不可比。**
- **不**宣稱 ratio 的臂間離散度——本區塊是它的**第一次**測量，n=1 個差值。

---

## 9. 讀出表（**資料進來之前寫好**）

| 格 | 臂 | 讀數 | 判定 |
|---|---|---|---|
| T-base1 | base 位置 1 | | |
| T-base6 | base 位置 6 | | |
| **漂移計 `\|Δ\|`** | | | ≤ 90 ms ⇒ 通過 |
| ratio-base（2 臂均值） | | | |
| ratio-Q（2 臂均值） | | | |
| **Q 效應** | | | §4 的標籤 + §5-Q |
| M 延遲-Q 臂（2 臂） | | | |
| M 延遲-M 臂（2 臂） | | | |
| **M 效應** | | | §5-M |
| `never` 比例（Q / M） | | | 右設限已排除，數量另報 |
| 偵測延遲分布 | | | 只報，不判 |
| 線性診斷 5:3:1 | | | 只在明顯違反時發言 |

---

## 10. 追加規則

- 被投影片引用的讀數要 `git add -f`（`.gitignore:60` 擋 `doc/audit/*/raw*/*`）；raw dump 維持 ignore。
  依據：`b024354` 抓到投影片逐字寫「both committed」而 git 從沒見過那兩個檔。
- **發現隨時落盤**，不等一輪結束。用量中斷、session 掛掉、context 壓縮都不給警告。

[Co-developed with claude code -- Adam]

---

# 增補 A（append-only，2026-08-28 09:40，**B1 與 Q2 已跑完、其餘四臂未跑；任何負載數值都尚未被讀取**）

## A-0. 為什麼要有這個增補

審查員指出（他給的設計，他自己抓到前提被違反）：

> 鏡像抵消的是**線性**漂移，而今天這台機器會產生**階梯**。
> 鄰居 VM（`開機手冊` 今天要序列跑兩臂 p4c/bmv2 編譯，估 4–5 小時；host 15 G 可用 4 G、swap 已用 8 G）
> 開始／結束一輪編譯**不是線性的**。六臂裡階梯落在哪個位置，就偏袒哪一邊——**鏡像對階梯沒有保護。**

§1 的抵消論證逐字寫著「**只要漂移在該區間內近似線性**」。**那個前提今天不成立。**

## A-1. 🔴 這個增補寫在讀取任何負載數值**之前**

**現在的狀態（可查證）**：`B1`、`Q2` 已跑完，`M3` 進行中，`M4`/`Q5`/`B6` 未跑。
**我沒有打開過任何一個 `load.jsonl`。** 判準訂完、commit 完，才會去看。

理由：階梯判準若在看過資料後才訂，它就只是「把我不喜歡的臂挑掉」的形式化。

## A-2. 偵測器只能用**六臂都有**的欄位

`run_arm_L.sh` 從第一臂起就每 5 s 跑 `sample_load.py`，六臂**完全相同**，記的是：

`loadavg`(1/5/15)、`/proc/stat` 逐 CPU 計數、`kernel_utime/stime/threads`、`top`(220 字/行)、`ncpu`

🔴 **記憶體與 swap 不在裡面**，而那正是鄰居 VM 最早出現的地方。
⇒ 09:40 另開 `log_host_memory.py`（`raw/host_mem.jsonl`，10 s 一次 `/proc/meminfo` + `/proc/vmstat`）。

⚠️ **它涵蓋臂 3–6，不涵蓋 1–2** ⇒ **它是解釋器，不是偵測器。**
「四個臂有、兩個臂沒有」的訊號**不能拿來裁決那兩者之間的比較**——那與「誤差項取錯母體」是同一個錯。
**偵測一律只用 loadavg 與 `/proc/stat`。** `host_mem.jsonl` 只在偵測器已經說有階梯之後，用來**指認機制**。

## A-3. 🔴 判準（數字寫在資料之前）

每臂在**自己的 T 量測窗內**算：
- `busy_a` ＝ `/proc/stat` 差分得到的**非 idle CPU 比例**平均
- `L1_a` ＝ 1 分鐘 loadavg 平均

每個條件取其兩臂的平均。**逐比較**判定（Q 用 base↔Q，M 用 Q↔M）：

| 條件間差距 | 判定 |
|---|---|
| `\|Δbusy\| > 0.10` **或** `\|ΔL1\| > 3.0` | 🔴 **該比較作廢**——被比的兩邊負載脈絡本身就不同，差值不可歸因於 binary |
| `0.05 < \|Δbusy\| ≤ 0.10` | ⚠️ **降級**：效應與負載差距**寫在同一句話裡**，不得單獨報效應 |
| `\|Δbusy\| ≤ 0.05` | 照原判準走 |

**條件內階梯**：若同一條件的兩臂 `\|Δbusy\| > 0.10`
⇒ 該條件報成**區間，不報平均**。🔑 **對階梯取平均正是讓階梯消失的動作。**

**作廢之後做什麼**：同 §3-3——**先重跑一次同區塊、新世代**，不改設計、不加臂。

## A-4. 為什麼「不用等鄰居跑完」是對的（審查員的話，我同意並補理由）

比較是**差異式**的，而且要的是**可偵測性不是無干擾**。
🔑 **「知道自己被干擾了多少」比「假設沒有被干擾」有用**——後者無法被否證。

補一條理由：等 4–5 小時之後機器**也不是乾淨的**（swap 已用 8 G 不會自己還回去），
所以「等」買到的不是乾淨，只是**未量測的髒**。

## A-5. top-k 排序鍵那一格：**不加進這個區塊**，另跑

審查員指出的第二件事**成立且重要**：
top-k 的排序鍵是 `estimated_packet_rate_in_the_proceeding_1sec_timeslot`（`FLUC:2339-2341`），
而 N-2 記著 baseline 上**正好只有這個欄位保留陳舊值**
⇒ **baseline 的 top-k ＝ 用「死流保留舊速率」的欄位，去排序「92% 是死流」的母體**
⇒ 死流會**贏過活流搶進前 K**，而 Energy-Saving-App 吃的正是這個端點。

🔴 **但這需要輪詢 `get_detected_top_k_flow_data`，而本區塊的六臂都沒有輪詢它。**
只替後四臂加 ⇒ **違反 §6「六臂工作點完全相同」**，而那正是鏡像設計唯一的依靠。

⇒ **登記為區塊之後的獨立一輪**（base ↔ 本分支各一臂，只看 top-k 前段有沒有死流）。
**不動現在正在跑的臂。**

[Co-developed with claude code -- Adam]

---

# 增補 B（append-only，2026-08-28 09:45，**M3 進行中、M4/Q5/B6 未跑；我沒有讀過任何 M 臂的延遲值**）

## B-0. 同一個校準母體問題，M 的區間也有（審查員急件，我同意）

我抓到他的 90 ms 門檻是拿**安靜臂**校準的。**同一個刀口對著我自己**：

M 的 **0.4–0.6 s** 區間是在**煙霧臂**校準的，而煙霧臂 `T ≈ 1024–1040 ms`；
本區塊 `B1` 量到 **`T = 1141.4 ms`**。

⇒ 若「拿到第一條路徑的延遲」隨**重算迴圈的實際週期**縮放，
**同一個機制在這個更重的工作點上，本來就會給出更大的效應。**
那時 >0.6 s **不是 miss，是機制在新工作點的正確表現**。

## B-1. 🔴 **區間不改**（改了預註冊就沒有預註冊），改成加一個診斷

判讀順序先寫死：

1. M 的效應落在 **0.4–0.6 s** ⇒ 照 §5-M 判，不動。
2. M 的效應 **> 0.6 s** ⇒ **先跑 B-2 的對帳，再決定它是不是 miss。**
   - 跟該臂自己的 `T` 一致 ⇒ 記成「**機制成立，區間的校準工作點與本區塊不同**」
   - **不**跟 `T` 走 ⇒ **這才是真的偏離**，回 §5-M 的「> 1.0 s」分支
3. M 的效應 **< 0.4 s** ⇒ 不受本增補影響，照原分支。

## B-2. 對帳用的估計量（**只用已經在收的資料，不新增測量**）

假設一條流在重算週期 `P` 內**均勻**出生：

| 估計量 | 式子 |
|---|---|
| `P̂_mean` | **2 × mean(latency)** |
| `P̂_p95` | **p95 / 0.95** |

🔴 **「均勻出生」是假設，不是觀察。** 它寫在這裡，是因為這種假設最容易在三段之後變成結論。
churn 的批次是每 5 s 一批 ⇒ 出生時刻對重算相位**不應該**有系統性偏好，但**沒有驗過**。
⇒ `P̂` 兩個式子若彼此差很多（>15%），**先懷疑這個假設，不要先下結論**。

## B-3. 煙霧臂已經給了先驗，所以可以寫出**互相排斥**的預測

`S_M2`（煙霧、M binary）：`T = 1024.2 ms`、mean 0.5736、p95 1.0813
⇒ `P̂_mean` = **1147 ms**、`P̂_p95` = **1138 ms**。

🔑 **兩個式子彼此吻合到 9 ms**（B-2 的假設在那一臂沒有出問題），
**但兩者都比同一臂的速率迴圈 `T`（1024）高約 120 ms。**
⇒ **重算迴圈與速率迴圈的週期看起來不是同一個數。**

本區塊（M 臂的 `T` 若同樣落在 ~1141）三個互斥預測：

| 假說 | 預測 `P̂` |
|---|---|
| **H-a** 重算迴圈週期 ＝ 速率迴圈 `T`（共用負載） | **≈ 1141 ms** |
| **H-b** 重算迴圈 ＝ 速率迴圈 ＋ ~123 ms 固定量 | **≈ 1264 ms** |
| **H-c** 重算迴圈是與負載無關的常數 ~1147 | **≈ 1147 ms** |

🔴 **誠實的限制先寫**：本區塊的 `T`（1141）**碰巧**落在煙霧臂的 `P̂`（1147）旁邊
⇒ **H-a 與 H-c 在這個區塊裡分不開**（1141 對 1147，差 6 ms，遠在噪聲內）。
**只有 H-b 可被這個區塊分辨。** 要分開 H-a／H-c 需要**第二個工作點**，本區塊沒有。

⇒ **不得**把「`P̂ ≈ 1141`」寫成「H-a 成立」。可以寫的是「**H-b 被排除／未被排除**」。

## B-4. 位階：診斷，不是門檻

與 §1-bis 的 5:3:1 同級。一個區塊的 p95 帶不動這個判斷。
🔴 **不因此改任何臂的參數、時長或工作點。**

📌 **若真的吻合，它的價值在於**：`T` 來自**分身邊值的階梯轉換時間戳**，
`P̂` 來自**每條流第一次拿到路徑的時刻**——**兩個獨立儀器指向同一個迴圈週期**，
比任一個單獨成立強。這與 `B1` 的「兩把尺對上」是同一種論證。

[Co-developed with claude code -- Adam]

---

# 增補 C（append-only，2026-08-28 10:22，**區塊 2 進行中（跑到臂 2/6），我沒有讀過區塊 2 的任何結果資料**）

## C-0. 這是**偏離預註冊**，所以理由與限制全部寫在讀資料之前

§3-3 對「作廢」寫的動作是「重跑一次、**換新世代**」。**區塊 2 沿用了同一個世代。**

**理由（審查員批准，並要求用我的版本而不是 3-3 的版本）**：
§3-3 是為「**漂移計說區塊無效**」那個情境寫的，**而那個情境沒有發生**——區塊 1 四道關卡全過。
⇒ 這不是作廢重跑，是**把一次意外變成對照實驗**。
換世代會多一個變因，把唯一有價值的配對比較毀掉。

## C-1. 🔴 宣稱要收窄一格：是 **CPU 競爭**，不是「那個 VM」

`開機手冊` 用 qemu monitor 的 `stop` 暫停 VM。**`stop` 只停 vCPU，guest 的記憶體配置仍在**
（他自己回報 VM 的 2.5 G RSS 沒有釋放）。

⇒ **區塊 2 的記憶體／swap 壓力沒有解除，解除的只有 CPU 競爭。**

**正確的宣稱：**

> **區塊1 − 區塊2 ＝ 那個 VM 的 *CPU 競爭* 做了什麼**

**不是**「那個 VM 做了什麼」。

✅ 這其實**對這個比較有利**：它把 CPU 單獨隔離出來，配對更乾淨。**只是名字要叫對。**

## C-2. 🔴 「其他都相同」的第二個例外：**世代老了 40 分鐘**

區塊 1 起 09:32:30、區塊 2 起 10:10:21。同一個世代，但**中間差 38 分鐘**。

相對於這個世代已經活了約 18 小時，38 分鐘很小，而且**兩個區塊各自帶自己的漂移計**（base 對）。
**但要寫出來，不要讓它藏在「同世代」這三個字裡面。**

## C-3. 預先寫死怎麼讀區塊 1 對區塊 2 的差

| 情況 | 讀法 |
|---|---|
| 兩區塊的主量（Q 的 ratio 差、M 的效應）**差異小於各自的臂間離散度** | CPU 競爭**沒有**移動結論。區塊 1 的數字可用 |
| 主量差異**大於**臂間離散度 | 🔴 **CPU 競爭移動了結論** ⇒ **以區塊 2（乾淨 CPU）為準**，區塊 1 降級為污染對照 |
| 兩區塊**同方向、量級相近** | 支持結論穩健，**但不得宣稱「證明無污染」**（見 C-1：記憶體壓力兩邊都在） |

⚠️ **`T` 本身預期會不同**（CPU 競爭直接影響迴圈週期），**那不是失敗，那是這個對照要量的東西。**

## C-4. 🆕 判準選擇的通則（審查員要寫進記憶，我同意並補一句）

> **選一個量當門檻之前，先問：「它會不會飽和？」**
> **會飽和的量，只能在未飽和區當門檻。**

本次實例：`busy` 有上界 1.0 且六臂 `busy_max` 全部 **＝ 1.000** ⇒ 它在整段量測裡都貼著天花板。
`load1` **沒有上界**，所以它看見了（27.13 → 31.48 → 30.02，逐字對上鄰居的時間表）。

🔴 **我把有上界的量當主判準、無上界的量當輔判準——這個順序是反的。**

**補一句**：飽和不只讓門檻失效，它還讓數字**看起來特別乾淨**
（`Δbusy` = 0.005 / 0.008）。⇒ **異常乾淨的判準讀數本身就是要查的徵兆**，
與「異常吻合的數字」同一類（本專案付過的代價：煙霧臂與①差 0.1 ms 被當成精度）。

## C-5. 🆕 「簽名缺席」不是放行（審查員要的第三面）

`T` 的方向與污染預測相反（B1 最少被壓卻最高、M4 全程被壓卻最低）。**我沒有拿它放行。**

三面併成一條：

| | 是不是免費的？ |
|---|---|
| 方向**不符** ⇒ 排除一條路徑 | ✅ **免費** |
| 方向**相符** ⇒ 採用一個機制 | 🔴 **不免費**（還要排除其他機制） |
| 🆕 簽名**缺席** ⇒ 放行 | 🔴 **不免費**——只排除**我想得到的**那條路徑，**而想不到的比想得到的多** |

[Co-developed with claude code -- Adam]

---

# 增補 D（append-only，2026-08-28 10:40，**區塊 2 的 B6 仍在跑；我還沒讀區塊 2 的任何結果**）

## D-0. 已知且有時戳的擾動落在區塊 2 的 `B6`

`開機手冊` 主動回報：**10:38:44 → 10:39:06（22 秒）** 一次 qemu 開機（I/O 最重那段），已 kill。
成因是他在測我加的 `exclusive_cpu` 守衛時，用**真的跑 `vm.sh start`** 去驗「應該放行」那一支。

**時間對帳（我算的）**：`B6` 穩定流 10:36:54 起、warm 20 s ⇒ **T 窗 10:37:14–10:40:34**。
⇒ 那 22 秒落在 **B6 量測窗的第 90–112 秒**，**窗長 200 s 的 11%**。**是 base 對照臂。**

## D-1. 🔴 判決一律照**已註冊**的關卡，不照敘事

**不新增門檻、不修改門檻。** 依序：

1. **§3-1 漂移計** `|T(B1) − T(B6)| ≤ 90 ms` — 若 B6 被推高，這一關會抓到。
2. **增補 A-3 階梯偵測** — 逐比較的 `Δbusy` / `ΔL1`。
3. **§3-2 ratio 漂移**。

| 結果 | 處置 |
|---|---|
| 三關**全過** | 區塊 2 成立，**擾動寫成限制**（已知有時戳 ≠ 不存在） |
| **漂移計失敗** | 區塊 2 的 `T` 讀數照 §3-1 作廢；**區塊 1（四關全過）維持主結果**，帶 C-1 的但書 |

## D-2. 額外的**定向**檢查（因為這次的擾動有時戳，而先前那次沒有）

`sample_load.py` 每 5 s 一筆 ⇒ 那 22 秒窗內有約 4–5 筆。
**看 `B6` 的 `load1` 在 10:38:44–10:39:06 有沒有相對於它自己的臂內基線跳起來。**

⚠️ **這是定向檢查，不是新門檻。** 它只能**指認機制**，不能推翻或取代 §3-1 的判決——
與 `host_mem.jsonl` 的定位相同（增補 A-2）。

## D-3. 🔑 兩個區塊同時存在，讓這件事的殺傷力小很多

C-3 已註冊：**若兩區塊的主量差異小於各自的臂間離散度 ⇒ 污染沒有移動結論。**

- 區塊 1：鄰居**全程** CPU 競爭（不對稱，壓在處理臂）
- 區塊 2：鄰居 CPU 已暫停，**只剩一次 22 s 的 I/O 尖峰**（壓在對照臂）

⇒ **兩次污染的形狀與位置都不同。** 若兩區塊的主量仍然吻合，
**那個吻合本身就是「兩種不同污染都沒有移動結論」的證據**，比任一區塊單獨成立強。

🔴 **反過來若兩區塊不吻合，不得挑一個喜歡的**：照 C-3，**以 CPU 較乾淨的區塊 2 為準**，
而 B6 的擾動要在同一句話裡講出來。

## D-4. 📌 `開機手冊` 的教訓值得原樣留著（與我今天的一條互為鏡像）

> **驗「放行」路徑不能靠真的讓它放行。**
> 拒絕路徑可以真跑——副作用就是「沒有副作用」；**放行路徑一跑就產生它要放行的那個副作用。**

這與記憶裡 [[smoke-the-accept-path-not-just-refusals]] 是**同一枚硬幣的兩面**：
那條說「只驗拒絕路徑不夠」，這條說「**驗放行路徑不能用真跑**」。
⇒ **兩條合起來才完整：放行路徑必須驗，但要用 dry-run 驗。**

[Co-developed with claude code -- Adam]
