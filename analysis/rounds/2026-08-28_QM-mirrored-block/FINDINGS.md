# 本輪的發現（**隨時落盤，不等一輪結束**）

`8/28 mainDev`，2026-08-28。新的在最下面。

---

## F-1 🔴 `pgrep -axf` 對任何**帶參數**的 process 一律回「不存在」

**兩個 session 今天都被它騙過**：審查員的現場回報寫「fabric 還活著但 **kernel 死了**，
`pgrep -axf 'ndtwin_kerne[l]'` 空」，我照抄同一條指令，也得到「(no kernel running)」。

**實際上 kernel 一直活著**，是昨晚留下的 `a40e04ce`（pid 1070106，由 `restart_kernel.sh`
在殺掉它之前印出 `running sha256: a40e04ce93b0e226…` 佐證）。

現場示範：

```
$ pgrep -axf 'ndtwin_kerne[l]'      # 空，exit 1
$ pgrep -af  'ndtwin_kerne[l]'      # 1567801 ./bin/ndtwin_kernel --mode mininet --topology …
```

**機制**：`-x` 要求**整條命令列**與 pattern 完全相等。加上 `-f` 之後比對的是 full command
line，而 kernel 的命令列是 `./bin/ndtwin_kernel --mode mininet --topology … --no-ai`，
永遠不可能等於 `ndtwin_kernel`。⇒ **`-x` 與 `-f` 合用，對任何有參數的程式都是恆假。**

🔑 **這是 false negative，而 false negative 比 false positive 貴**：
「它不在」會**驅動動作**（我差點在已經有 kernel 的機器上再起一顆），
「它在」只會讓你多等一下。

📌 與既有記憶的關係：
- `run_plane.sh` 早就記著**同一族**的坑（`pgrep -a simple_switch_grpc` 因 comm 15 字元上限而恆空），
  但那條的解法是「**加** `-f`」；這裡是「**`-f` 加上 `-x` 之後又壞掉**」。**兩條方向相反，要分開記。**
- 這是 [[process-liveness-checks-lie-in-two-ways]] 的**第十種**機制。

**這一輪的實際後果**：沒有。`restart_kernel.sh` 用 pid 檔而不是 pattern 找舊 kernel，
而且在起新的之前斷言 `:8000` 沒被佔住 ⇒ 兩層都擋得住。**是儀器騙人，不是機器壞掉。**

---

## F-2 ✅ 10 Hz 輪詢在**閒置** fabric 上完全不掉（M-quater (ii-i) 的前置條件，閒置側通過）

M-quater 要求：北向 API 一次只服務一個請求，所以 10 Hz 輪詢**可能自己就是擾動源**，
**這一步不做，整組 M 的數字不可用**。

閒置基線（60 s、無流量、binary `a40e04ce`）：

| 量 | 值 |
|---|---|
| polls | **600**（0 失敗） |
| 達成間隔 median | **0.1001 s** |
| p95 | **0.1001 s** |
| **max** | **0.1002 s** |

⇒ 要求的 10 Hz 拿到了，**最壞的一次也只差 0.2 ms**。

⚠️ **這只證明閒置側**。有負載時 API 會不會慢下來，證據在**每一臂自己的 `poll.jsonl`**
（逐次記 `ms` 與時間戳），read-out 會把兩者並排。**不要拿這一格代替那個比較。**

⚠️ 基線跑在 `a40e04ce` 上，不是三顆 binary 的哪一顆特別選的——它是昨晚留在機器上的那顆（見 F-1）。
對「輪詢器打得動幾 Hz」這個問題不影響，但**要寫出來**，因為 [[benchmark-must-name-the-binary-it-measured]]。

---

## F-3 儀器缺陷（我自己的）：baseline 的標題在檢查之前就印了

`drive_block.sh` 先 `say "--- poll-only baseline ---"`，**才**檢查 kernel 在不在。
kernel 不在時會走「deferred」分支，但**標題已經印出去了** ⇒ log 讀起來像基線跑過了。

這一輪沒有踩到（因為 kernel 其實活著，見 F-1），但它就是
[[failures-that-report-success]] 的形狀：**驗收寫在標題上，不是寫在狀態上**。已修。

---

## F-4 ✅ 煙霧臂：**穩定流＋churn 同時跑**成立，而且儀器分得出 M 的效應

這個工作點是**新組合**（先前 Q 只跑穩定流、M 只跑 churn），所以先跑縮時版
（2 臂、`FLOW_S=70`、churn 40 s），**走同一條程式路徑**。原始資料在 `raw/smoke/`。

| | `S_B1` = base `3367d0e9`（1 kHz） | `S_M2` = M `a40e04ce`（1 Hz） |
|---|---|---|
| churn 搬的資料 | 16 條、0 失敗、51 MB | 16 條、0 失敗、57 MB |
| 分母自證 | ABSENT（如預期） | ✅ **3/3 live, worst 0.0000%** |
| `T` | 1039.6 ms | 1024.2 ms |
| path 延遲 n | 41 | 46 |
| **mean** | **0.0594 s** | **0.5736 s** |
| median | 0.1001 s | 0.5554 s |
| p95 | 0.1070 s | 1.0813 s |
| max | 0.2052 s | 1.2092 s |
| `never` | 0 | 0 |

⇒ **效應 ＝ +0.514 s，落在 M-quater 預註冊的 0.4–0.6 s 區間內**，
而且是儀器解析度（輪詢 0.1 s）的 **5 倍**。

📌 **要先寫下的兩件事，免得正式區塊出來之後才「發現」**：

1. **1 kHz 臂的 median 是 0.1001 s，不是 0** ——那正好是**一個輪詢週期**。
   ⇒ 這格量到的是**儀器的下限，不是系統的延遲**。預註冊寫的就是「量不到」，**它確實量不到**。
   🔴 **不得把 0.0594 當成「1 kHz 的真實延遲」去和 0.5736 相減報成效應。**
   可以報的是「**M 臂 0.57 s，base 臂在解析度以下**」。
2. **M 臂的 p95 ＝ 1.081 s，超出預註冊的「p95 < 1.0 s」。** 煙霧臂 n=46，不足以判定，
   但**已經記在這裡**，正式區塊若重現就是一個要處理的區間外事件（PREREG 5-M：
   > 1.0 s ⇒ 重算迴圈的實際週期不是 1 s，與工單 Q 同形狀）。

⚠️ **煙霧臂不是結果。** 它只證明管線會動、儀器分得開。正式的六臂區塊才是要報的東西。

## F-5 10 Hz 輪詢在**有負載**時會退化，但退化量遠小於效應

| | 閒置基線 | `S_B1` 臂內 | `S_M2` 臂內 |
|---|---|---|---|
| gap median | 0.1001 s | 0.1001 s | 0.1001 s |
| gap p95 | 0.1001 s | **0.1178 s** | **0.1177 s** |
| **gap max** | 0.1002 s | **0.2798 s** | **0.4745 s** |
| 失敗輪詢 | 0/600 | 0/678 | 0/670 |

⇒ 中位數完全不動，**尾巴會拖**（最壞 0.47 s）。這正是「北向 API 一次只服務一個請求」的形狀。

🔑 **這就是為什麼每條流各自記 `gap_before_first_seen_s`**：全域「我們用 10 Hz」會把這條尾巴藏起來，
而**單一次 0.47 s 的空洞落在某條流的出生時刻，那條流的延遲就只知道到 ±0.47 s**。
統計量（p95）對它要防的故障是盲的——分析時要逐流看，不是看平均輪詢率。

---

## F-6 🔴 漂移門檻的校準母體與本區塊的工作點**不同**（**在 B6 跑出來之前寫**）

第一臂 `B1` 的 `T` ＝ **1141.4 ms**，比煙霧臂的 1039.6 高 100 ms——
因為工作點更重（16 條穩定流 **＋ 144 條 churn**，煙霧只有 16 條 churn）。

⚠️ **而 PREREG 3-1 的 90 ms 門檻，是從『安靜臂』推出來的**
（gen 1 的 27.2、gen 2 的 41.6，兩者都是 16 流、**無 churn**）。

⇒ **門檻的校準母體是安靜臂，本區塊不是安靜臂。** 重負載下臂間離散度可能更大。

🔴 **門檻不改。** 預註冊就是預註冊，看到數字之後調門檻就是它存在的意義被取消。
但**這個但書必須現在寫**——寫在 `B6` 之前，否則萬一 `|ΔT|` 落在 90–150 ms，
它就會變成一個**事後找到的藉口**，而不是一個**事前知道的限制**。

📌 **`|ΔT| > 90 ms` 時的正確讀法**：不是「門檻訂錯了」，是
**「這是第一次在重負載下量臂間離散度，而它比安靜臂大」**——那本身就是一個發現，
而且它會改變**未來**每一個區塊的臂數需求。照 PREREG 3-3 執行（重跑一次、新世代）。

---

## F-7 ✅ `ratio` 與 `T` 各自獨立量到、而且互相對上（`B1`，base binary）

| 量 | 值 |
|---|---|
| `T`（外部階梯轉換估計） | **1141.4 ms** over 174 gaps |
| ⇒ 1000 ms 硬編碼分母的預測高報 | **+14.1%** |
| ratio host→switch | **1.1470** |
| ratio switch→host | **1.1591** |
| ratio switch→switch | **1.1485** |

三個類別全部落在預註冊區間 **1.1414 ± 0.02 ＝ [1.121, 1.161]** 內。

🔑 **兩邊沒有共同來源**：`T` 來自**分身邊值的階梯轉換時間戳**，
`ratio` 來自 **veth 位元組計數對分身回報的位元組**。機制若是錯的，兩者只會各說各話。

⚠️ **這是一個臂**（n=1），而且**還沒有第二個 base 臂可以當誤差項**。
在 `B6` 進來之前，**這不是效應量，只是「兩把尺對上了」**。
（本專案上一次把單臂讀數當成精度，付出的代價是 `10.9 SE` 其實是 `0.31`。）

---

## F-8 🔴 **撤回**：我註冊的 chaos 差異臂預測，前半是反的——baseline 從來不保留陳舊速率

`開機手冊` 更正（`06287c2`），**我自己複驗過才收下**（不是照單全收）：

我在 `NEXT.md:53-54` 寫的：

> baseline 的 `get_detected_top_k_flow_data` 在流量停止後 5–10 秒仍會回報非零、
> 逐位元相同的速率；本分支會回報 0。

**後半對，前半反了。**

### 我自己跑的複驗

```
git show origin/main:src/.../FlowLinkUsageCollector.cpp   :1453-1459
    if (hopsCounter == 0) {
        // No active hop in this interval, so explicitly clear periodic rates.
        info.estimatedFlowSendingRatePeriodically = 0;
        info.estimatedPacketSendingRatePeriodically = 0;
        continue;
    }
git merge-base --is-ancestor 31b357a HEAD  → YES    origin/main → no
git merge-base --is-ancestor aabe605 HEAD  → YES    origin/main → no
```

| commit | 死流的 `_in_the_proceeding_1sec_timeslot` |
|---|---|
| `origin/main` | **清成 0**（一直都是） |
| `31b357a`（本分支，07-27） | **保留舊值** ⇐ 除零守衛的 `continue` 跳過了清除 |
| `aabe605`（本分支，08-20） | **清成 0**，修回來了 |

⇒ **兩個分支今天都清成 0。差異臂原本要問的那件事，在碼上已經沒有差異。**

### 🔑 誤讀是怎麼發生的：**我讀到註解的一半就停了**

我引的依據是 `FLUC:1896-1910` 的註解，它**確實**記著實測
「iperf3 結束後 5 秒與 10 秒仍回報逐位相同的 20.3 Mbps / 10496 pps」。

**而同一段註解的最後一句是：**

> **Introduced on this branch by the divide-by-zero guard (31b357a6), so it is ours to fix.**

**那次量測量的是我們自己的壞窗口。** 我讀了量測、沒讀歸屬，
於是「**我們造成的**」變成「**baseline 的性質**」。

📌 這與記憶裡「**沒讀全文就叫別人讀**」是同一形狀，
但更難防：**證據是真的、數字是真的、行號是對的**——錯的是**它屬於誰**。
⇒ 這是 [[cited-line-numbers-are-not-evidence]] 的變體：**引到的行是真的，但那一行講的是另一個分支。**

### 立刻的後果（三個）

1. 🔴 **`NEXT.md:59` 我自己註冊的觸發條件已經滿足**（「若 baseline 沒有重現 ⇒ `d38d209`
   裡整段對 baseline 的歸因要撤回」）。**baseline 在碼上就不會重現 ⇒ 那段現在就撤回，不必等臂。**
   `d38d209` 的「嚴重度分支相依」**不成立**：兩個分支的死流速率都是 0。
   ⚠️ `d38d209` 的**另一半**——「模擬器逐流消費這個端點」——**不受影響，仍然成立**。
2. 🔴 **審查員的「加乘效應」不成立**：他推論「top-k 用保留舊值的欄位排序 ⇒ 死流搶進前 K」。
   兩個分支都清成 0 ⇒ **死流排到最底**，不是搶進前面。
3. **差異臂仍值得跑，但要換問題**（`開機手冊` 的版本，我同意）：
   `getTopKFlowInfoJson` 取 `min(k, size)` 且**不過濾**（`FLUC:2347`、`k` 預設 50）
   ⇒ **兩臂都應回約 50 筆、其中約 45 筆速率 0**。
   **清單不是被死流「灌到前面」，是被屍體填滿**——那是 W 的**母體**問題，兩個分支都有。

### ⚠️ 一個我**沒有**驗、但推論得到的

baseline binary `3367d0e9` 的原始碼**應該**包含 `aabe605`（08-20），
因為它是①／P／Q_T64（08-25 之後）量的那顆。**這是日期推論，不是實測。**
若要當結論用，去驗那顆 binary 的來源 commit。

### 📌 教訓（`開機手冊` 寫的，我原樣收下並加一句）

> **要把兩個缺陷相乘之前，先確認兩個都還活著。**

加一句：**兩個缺陷之一是我自己一小時前寫進預註冊的，而我沒有回去查它是否還活著。**
預註冊保護的是「不要事後改判準」，**它不保護「當初寫下去的前提就是錯的」。**

---

## F-9 🔴 我的階梯偵測器**通過了，而它不該通過**——`busy` 在飽和的機器上是瞎的

`開機手冊` 回報（10:0x）：他的 QEMU VM 在**我整個區塊 1 的量測窗內**滿載編譯 p4c／grpc／PI，
4 vCPU，而且**形狀是中間重、兩端輕**：

| 他的作業 | 起訖 | 落在我的第幾臂 |
|---|---|---|
| v10 NOTTY | 09:23–09:36 | 壓到**臂 1 尾巴** |
| v10 TTY | 09:40–**09:56** | **臂 2、3、4、5** |
| v8 乾淨室（VM 10:01:34 開機） | 09:58– | **臂 6** |

⇒ **兩個 base 控制組受影響最小，四個處理臂受影響最大。**
**鏡像抵銷的是漂移，抵銷不了這個形狀。**

### 🔴 我的偵測器為什麼漏掉它

```
arm  cond   busy   busy_max   load1
B1   base  0.867     1.000    27.13   ← 最低
Q2   Q     0.869     1.000    29.96
M3   M     0.878     1.000    31.04
M4   M     0.885     1.000    31.48   ← 最高
Q5   Q     0.879     1.000    28.28
B6   base  0.872     1.000    30.02
```

`Δbusy`：base↔Q **0.005**、Q↔M **0.008**。**乾淨到不可思議——那就是徵兆。**

🔑 **每一臂的 `busy_max` 都是 1.000 ⇒ CPU 早就頂到天花板。**
飽和之後再加負載**不會抬高 `busy`**，只會拉長 run queue、讓所有東西變慢。
⇒ **我把主判準訂在一個有上界的統計量上，而它要防的故障正好發生在上界之上。**

**而 `load1` 有看到**：27.13 → 31.48 → 30.02，**中間重兩端輕，逐字對上他的時間表**。
`ΔL1(Q,M)` = **2.14**，門檻 3.0 ⇒ 過關，**但邊際遠比 `busy` 讓人以為的窄**。

📌 這是 [[instrument-must-not-mimic-its-own-finding]] 的**新一式**：
不是「失敗預設值長得像量測值」，是 **「統計量有天花板，而故障住在天花板之上」**。
與已記的「p95 看不見單一空洞」同族，但機制不同：**那個是分位數不敏感，這個是量本身飽和。**

🔴 **判準不改（區塊 1 的關卡確實是過的），但偵測器要改**：
飽和機器上正確的量是 **run queue 長度／`steal`／每單位工作的完成時間**，不是 CPU 佔用率。

### ⚠️ 污染有一個可檢驗的簽名，**而它沒有出現**——但這不足以放行

若 kernel 被搶 CPU，迴圈週期 `T` 應在臂 2–5 偏高。實測：
B1 **1141.4**（他說最少被壓）、M4 **1063.1**（他說全程被壓）⇒ **方向相反**。

**不拿它當「所以沒事」。** 它只說污染沒有走我想得到的那條路徑，
而**想不到的路徑比想得到的多**。

### ⇒ 處置：重跑，而且**故意沿用同一個世代**

PREREG 3-3 的作廢動作是「重跑、換新世代」。**這次刻意不換，理由先寫**：

區塊 1 與區塊 2 若**只差鄰居的 VM 負載**（同世代、同 binary、同工作點、同 seed、同腳本），
**兩者之差就直接量到那個 VM 對這組結果做了什麼**。換世代會多一個變因。

🔑 **這不是 3-3 的作廢重跑**（區塊 1 過關），**是把一次意外變成一個對照實驗**。
區塊 1 完整保留在 `raw_block1/` 與 `L/raw/*_block1/`，且已 commit。

### 📌 協定層的教訓（比這次的數字重要）

> `.test_run/lab.claim` 保護 fabric 與 build，**保護不了 CPU**。

而**這件事記憶裡已經有了**（`vm-on-this-machine-is-invisible-to-ndt-status`：
「網路不撞但 CPU/RAM/I-O 會搶，而沒有機制會告訴別人」）——**它還是發生了。**

🔑 **知道 ≠ 有機制。** 記憶擋不住它，因為 **claim 協定沒有把它編碼進去**。
「開 VM 前先講一聲」依賴雙方記得；**該加的是可查詢的那一層**——
開 VM 前 `cat .test_run/lab.claim`，就像我們對 fabric 做的一樣。

---

## F-10 ✅ `3367d0e9` 的來源 commit：**實測，不再是日期推論**

F-8 我寫過「`3367d0e9` **應該**包含 `aabe605`，因為它是 08-25 之後量的那顆——**這是日期推論，不是實測**」。
今天它變成載重的問題（top-k 那輪的預期完全取決於它），所以**去量了**。

**方法**：`aabe605` 在 `HttpSession.cpp` / `FlowDispatcher.cpp` 引入了新的字串常值 ⇒ 直接查 binary。

| 字串 | 引入於 | `3367d0e9` | `ab2d7ed1` | `a40e04ce` |
|---|---|---|---|---|
| `FlowDispatcher is stopped; dropping` | `aabe605`（08-20） | **PRESENT** | PRESENT | PRESENT |
| `recorder is only started outside MININET mode` | `aabe605` | **PRESENT** | PRESENT | PRESENT |
| `rate divisor check` | `f5e3556`（08-27，工單 Q） | **absent** | PRESENT | PRESENT |

🔑 **第三列是負控制**：它證明這個測試**分得出東西**，不是對每個字串都回 PRESENT。
（沒有負控制的話，「三顆都 PRESENT」與「我的 grep 壞了」長得一模一樣。）

⇒ **`3367d0e9` 建置自 `aabe605` 之後的 commit ⇒ 它會把死流的 periodic 速率清成 0。**
⇒ **top-k 那輪的兩臂都在「清零」那一側。** 這是**由位元組決定的**，不是由「baseline」這個詞決定的。

---

## F-11 🔑 top-k 實測：**兩個假說都不對，真相是第三個**

**兩臂**：`W_base` = `3367d0e9`、`W_branch` = `a40e04ce`。churn 120 s ＋ 停流後 40 s，1 Hz 記名次。

⚠️ **這一輪的預期沒有乾淨地預註冊**，因為前提在一天之內被翻了三次
（我的 `6bcd4fc` 撤回 → 審查員的急件反駁 → 審查員自己撤回）。**我把這件事寫出來，不假裝有。**
**唯一在讀資料之前確定的是 F-10 的 binary 身分**，而那是由位元組定的。

| | `W_base` (3367d0e9) | `W_branch` (a40e04ce) |
|---|---|---|
| 有流量時列出的筆數（median） | **33** | **34** |
| 其中已結束的（median） | **27** | **27** |
| **死流且排序鍵非零** | **0** | **0** |
| 死流出現在前 10 名的次數 | 550 / 3040（18%） | 463 / 3035（15%） |
| **每次輪詢前 10 名裡的死流（median）** | **4 / 10** | **2 / 10** |
| 停流 5 s 後的筆數（median） | **0** | **0** |

### 三個宣稱各自的下場

1. 🔴 **審查員的「死流帶著陳舊非零速率搶進前 K」**：**否證**。
   `dead-AND-nonzero-sortkey` 在**兩臂都是 0**——與 F-10 一致（兩顆 binary 都清零）。
2. 🔴 **我的「兩臂相同 ⇒ 缺陷與分支無關 ⇒ 沒事」**：**也不對**。
   兩臂**確實**相同，但那不代表沒有缺陷。
3. ✅ **真相**：**死流的排序鍵是 0，但它們照樣占住前 10 名**——
   因為**同時活著的流根本不到 10 條**，所以第 7–10 名是拿零值的屍體填的。

> **它們不是「贏過」活流，是「填滿」活流下面的空位。**
> 消費端讀「前 10 名」拿到的是 **約 4 條真的流 ＋ 約 6 具速率為 0 的屍體**。

🔑 **這與「灌爆清單」和「搶進前段」都不同，而且它是兩個分支都有的母體缺陷**——
`getFlowInfoJson` 的走訪**零存活性過濾**，`getTopKFlowInfoJson` 取 `min(k, size)`。
**就算速率欄位全部正確歸零，前 10 名裡仍有 6 具屍體。**

⚠️ **不宣稱** 4/10 對 2/10 的差是 M 造成的：**每邊只有一個臂**，且兩顆 binary 在這條路徑上沒有機制差異。

### 📌 這一輪最該記的

**同一個事實在一天內被三個 session 講成三種版本，而爭執從來不是行為，是「baseline 指哪一個 commit」。**

| 講「baseline」時可能指 | `…RatePeriodically` 在 `hopsCounter==0` 時 |
|---|---|
| `28b8b13`（fork point） | **保留**（審查員讀的） |
| `origin/main`（今天） | **清除**（我讀的，`8b61cdc` 改的） |
| **`3367d0e9`（臂真正跑的）** | **清除**（F-10 實測） |

> 🔴 **凡是宣稱「baseline 如何」，都必須指名 commit。不指名的一律作廢，不管它說什麼。**

**三個人都讀了正確的原始碼，回答了不同的問題。**

---

## F-12 🔴 六個實驗量過這顆 binary，而**沒有任何紀錄能把它接回原始碼**

審查員去查 `3367d0e9` 是哪個 commit，**三條路全部不通**：

| 路徑 | 結果 |
|---|---|
| binary 內嵌的 40-hex `961c151d…` | **不是 git 物件** |
| ELF BuildID `d8ae7ac4…` | 建置產物，與原始碼無關 |
| `doc/` 裡 6+ 個引用它的檔 | **全部只記 sha256，沒有一個記 commit** |

🔑 **這是「Benchmark 要指認量到的 binary」的下一層**：
我們對**位元組**極度嚴謹（到處 sha256、還拿 `/proc/<pid>/exe` 驗），
對**它是哪份原始碼**完全沉默。**sha256 識別的是位元組，不是來源。**

### ⇒ 但那個載重的問題**不需要 commit id**（見 F-10）

我要的從來不是 commit，是「**它清不清零**」。字串簽名（靜態）＋ top-k 實測（動態）
**兩條沒有共同來源的線都說「清零」**。

📌 **規則的細化**：「宣稱 baseline 如何必須指名 commit」——
**而當 commit 查不到時，指名『你依賴的那個行為』並直接量它，是合法的替代。**

### 已做：`.test_run/binaries/<name>.provenance`（三顆都寫了）

`commit=UNKNOWN` 與 `dirty_worktree=UNKNOWN` **是量測結果不是佔位符**，
註解寫明三條路都不通、且**明確禁止用 mtime 推**（08-26 00:34 是建置或複製時間）。

🔴 **`dirty_worktree` 獨立一行**，理由是審查員的：
**從髒工作區建出來的 binary，連 commit 都不足以識別它。**
⇒ 以後要在**建置當下**由 `git rev-parse HEAD` ＋ `git status --porcelain` 寫入。

---

## F-13 `cmd && A || B` 把三值壓成二值——**這比「判準沒留以上皆非」更根本**

審查員查 F-12 時寫：

```bash
git merge-base --is-ancestor aabe605 961c151d && echo "有修正" || echo "沒有修正"
```

印出「**沒有修正**」，而真正原因是 **`961c151d` 不是 git 物件**——**指令是因為第三個理由失敗的**。

🔑 **`--is-ancestor` 的 exit code 本來就是三值的**（0 是、1 否、**其他＝錯誤**）。
**工具給了第三個值，而 `&& ||` 的語法把它摺進了「否」。**

⇒ 這不只是「判準要留『以上皆非』」（那是**設計**判準時的事），
**這是工具已經給了第三個值，而我們的語法丟掉了它**——是**讀取**判準時的事。

📌 與 [[instrument-must-not-mimic-its-own-finding]] 完全同構：
**`&& ||` 讓「失敗」與「答案是否」變成同一個輸出**，
和「`0` 既是合法量測值又是失敗預設值」是同一個形狀。

**修法**：
```bash
git merge-base --is-ancestor A B; rc=$?
case $rc in 0) echo yes ;; 1) echo no ;; *) echo "ERROR rc=$rc -- not an answer" ;; esac
```

---

## F-14 🔴 **記錄實驗這個動作本身，是量測窗裡的干擾源**——而大部分是我造成的

審查員發現 Adam 有一個 **post-commit hook**，每個 commit 在背景跑一次 LLM code review。
**我複驗過**：`.git/hooks/post-commit` 存在，執行 `agy --model gemini-3.1-pro-high --print-timeout 900s`。

### 我獨立數了一次，數字與他的不同，**而差異改變了他的論證**

| 窗 | 審查員數的 | **我數的（`git log --since/--until`）** |
|---|---|---|
| 區塊 1（09:32:30–10:03） | 5 | **8** |
| 區塊 2（10:10:21–10:41） | 6 | **6** |

⇒ 他據此說「**區塊 2 的 commit 比區塊 1 多 ⇒『乾淨 CPU』的框架是錯的**」。
**實際上區塊 1 比較多（8 對 6）。** 他的**結論方向要反過來**：
就這個軸而言，**區塊 2 仍然比區塊 1 乾淨**，只是**乾淨的幅度比「鄰居 VM 暫停」暗示的小**。

🔴 **但他要求改措辭這件事完全成立**：不能寫「clean CPU」，
要寫「**鄰居 VM 的 CPU 暫停，但兩個區塊都有 commit 觸發的背景 review，區塊 1 有 8 次、區塊 2 有 6 次**」。

### 🔑 15 個 commit 裡有 12 個是我的

**而它們正是「發現隨時落盤，不要等一輪結束」那條規則的產物**——
那條規則是我自己寫進 handoff、審查員又要我再寫一次的。

> **一個為了不遺失證據而設的規則，在量測窗裡製造了干擾。**

⚠️ **這不是說那條規則錯了。** 用量中斷與 context 壓縮**真的**會吃掉沒寫下的東西，
而那個風險比 1/4 核的間歇負載大得多。
**要改的是時機**：**量測窗內累積、窗與窗之間落盤**（臂間有 20 秒靜置，而 `settling` 那段正好是安全的窗口）。

### 對 jitter 那輪是硬性的

jitter 量的是**變異數**，而這個干擾源**間歇、每次可達 10 分鐘、幅度約 1/4 核**
⇒ **jitter 量測窗內禁止任何 session commit**。這條要寫進那輪的預註冊。

### 標記（照審查員自己的分級）

- **實測**：hook 存在且內容如上；15 個 commit 落在窗內（我數的）；每個 commit 一個 review 檔
- **他的實測**：`agy` 瞬間 26.7% 一顆核；review 會重疊（0659 比 0660 早取號晚 11 分鐘完成）
- **仍是推論**：每次 review 的**持續** CPU 與總貢獻——**只量了一個瞬間，沒有量曲線**

⇒ 🔴 **在把曲線量出來之前，不得用它解釋任何具體的臂間差異**，只用來修正「乾淨」這個措辭。

---

## F-15 把 F-14 的推論變成實測——**而我的第一個儀器量到的是它自己**

### 🔴 先講失敗的那個：`pgrep -f 'ag[y]'` 匹配到我自己的取樣器

我寫了一個 per-process 取樣器要量 review 的 CPU 曲線。它報「9–12 個行程、mean 0.048 核」。

**那個數字不能用**，因為取樣器的 argv 是
`python3 …/sample_ag**y**.py …/ag**y**.jsonl 150`——**裡面有字面的 `agy`**，
所以 pattern 匹配到它自己。而且**我只記了行程數與 CPU，沒記 cmdline**
⇒ **事後無法把自己減掉。**

🔑 這是今天第 N 次同一族（`pkill -f` 殺自己的 shell、`pgrep -axf` 恆假），
但**新的一面**：bracket 技巧（`ag[y]`）保護的是 **pattern 本身**，
**保護不了 argv 裡其他地方出現的同一個字**——**我的輸出檔名就是那個洞。**
📌 而**「沒記 cmdline」才是真正致命的**：有數字、無身分 ⇒ 不可歸因 ⇒ 整份資料作廢。

### ✅ 換一個不需要新儀器的方法：hook 自己留下的檔案

hook 每個 commit 寫一個 `.git/agy-reviews/<seq>-<sha>.md`。
⇒ **`commit 時間 → 檔案 mtime` 就是那次 review 的實際時長**，用的是**已經存在的產物**。

**兩個量測窗內全部 14 個 commit：**

| | |
|---|---|
| review 時長 | **35–43 s**，全部（中位數 ~38 s） |
| 輸出大小 | 108–111 bytes，全部 |
| **有任何兩次 review 重疊嗎** | **沒有**（最近的兩個 commit 相隔 38 s，review 36 s，差 2 s 錯開） |

**佔空比**：
- 區塊 1：8 次 × ~37 s ＝ **296 s / 1830 s ＝ 16.2%**
- 區塊 2：6 次 × ~40 s ＝ **240 s / 1860 s ＝ 12.9%**

### 🔴 這修正了審查員的推論

他寫「**區塊進行中的大部分時間，都有一個以上的背景 review 在燒 CPU**」。
**實測：窗內 13–16% 的時間有恰好一個 review，從來沒有兩個。**

他觀察到的重疊（`0659` 跑 1688 s、`0660` 跑 1427 s）**發生在 10:56–11:24**，
**兩個區塊都結束之後**——那時 commit 變密、review 開始排隊。
⇒ **他量到的是真的，但那是窗外的狀態，不是窗內的。**

🔴 **而他要求改措辭這件事仍然成立**：不寫「clean CPU」，
寫「**鄰居 VM 的 CPU 暫停，兩個區塊各有 8 次與 6 次 commit 觸發的背景 review，
每次約 38 秒、互不重疊、佔窗長的 16% 與 13%**」。**現在這句話每個數字都是量到的。**

### 📌 順手撿到：兩次 review **逾時了，而且回報成功**

`0659`（77 bytes / 1688 s）與 `0660`（77 bytes / 1427 s）——
正常是 ~109 bytes / 38 s。hook 的參數是 `--print-timeout 900s`。
⇒ **那兩次幾乎確定是逾時**，而 `agy` **逾時仍 exit 0**（既有記憶已記）。
⇒ **兩份 77 bytes 的檔案就是「失敗回報成功」的實體證據**，就躺在 `.git/agy-reviews/` 裡。

⚠️ **不宣稱** 每次 review 的 CPU 幅度——**那仍然是推論**（審查員量到一個瞬間 26.7%，
我的曲線量測作廢）。**時長與重疊是實測，幅度不是。**

---

## F-16 🔴 **這個 remote 設定讓「什麼是公開的」在結構上無法從本地 ref 得知**

```
p4  fetch = git@github.com:Adam010341/NDTwin-Kernel-P4.git      ← PRIVATE
p4  push  = git@github.com:Adam010341/NDTwin-Kernel-P4.git      ← PRIVATE
p4  push  = git@github.com:ndtwin-lab/NDTwin-Kernel-P4.git      ← 🔴 PUBLIC
```

⇒ **`git push p4` 會發佈到公開 repo，而 `p4/main` 反映的是私有 repo。**

🔑 **不管多仔細看 tracking ref，都看不到公開狀態。這不是粗心，是設定造成的結構性盲區。**

**唯一可靠的做法**：直接對公開 URL 打 `git ls-remote` 或 `gh repo view`。
我最後就是這樣才確定的——**而我第一次的結論（答案對）是從 `p4/main` 推的，路徑是錯的。**

### 實測的公開曝光量（我自己數的，與審查員獨立吻合）

| 樹 | 檔案數 |
|---|---|
| `main` / `fix`（`20cd80b`） | **999**（`doc/audit/` 佔 **563**） |
| **`audit-raw`（`4ffe9c5`）** | **2,743**，其中 **2,742 是 raw dump** |
| **合計** | **約 3,700** |

🔴 **`audit-raw` 是一條專門用來裝 raw dump 的分支** ——
`.gitignore:60` 擋 `doc/audit/*/raw*/*` 是為了不讓 raw 進 `main`，
**而這條分支繞過了它，並且是公開的。只看 `main` 的清理會漏掉四分之三。**

主樹揭露項（我的計數）：**57** 個檔案指名 session／角色、**168** 個含 `/home/adam`。

### 📌 兩件必須和選項一起呈給 Adam，不能只給選項

1. **`NDTwin-Kernel` 從 08-26 就公開了** ⇒ 那不是今天的事，**可能是他早就知道且刻意的**。
   若是，路徑與版本字串那兩件他很可能本來就接受（author email 一直都在裡面）。
2. **公開可能是他自己裁的** ⇒ **「轉回 private」等於撤銷他的決定**，更不能由我們發動。

⇒ **問題要問成**「已經公開 N 小時、內容是這些（附數字），要維持／轉回 private 整理／其他」，
**不要問成「要不要關掉」。**

### 🔑 兩個 session 都給了假的安全感

審查員對兩個 session 下「不要 push」，我對他說「commit 安全、push 不安全」
——**而那時已經 push 且公開三小時**。

> **在給別人一個「不要做 X」的指示之前，先確認 X 還沒發生。**

⚠️ **擋錯地方比不擋更糟**：它讓收到指示的人以為擋住了。

---

## F-17 🔴 更正我自己：**「已公開三個多小時」沒有證據支撐**，而且授權鏈是完整的

### ① 授權：**完整，不需要補票**（`開機手冊` 回答，Adam 直接說了三次）

1. 選擇題問「§6.2–§6.7 的 P4 檔案不在公開 repo，走哪條路」→ 他選**公開 `NDTwin-Kernel-P4`**
2. 他**自己打字**：「幫我開分支，audit可以全部公開」
3. 🔑 **在我列的三件被逐項查證並附上數量之後**，再問一次「公開不可逆，這三類怎麼處理」
   → 他選**「全部照原裁定，直接公開」**

⇒ **第 3 次正是針對我列的那三件，他看過數字才確認。** 而且**轉 public 是他自己執行的**
（`開機手冊` 只推了 `main`，改預設分支與轉 Public 兩步是 Adam 自己做的）。

### ② 🔴 我的錯：**`pushedAt` 不是 visibility 的變更時間**

我寫「`ndtwin-lab/NDTwin-Kernel-P4` **今天 10:47:45 起公開**」。
**那個時戳是 `開機手冊` 推 `main` 的時間**，不是轉為公開的時間。
**`gh` 根本不提供 visibility 的變更時間** ⇒ **可能更早、可能更晚，我不知道。**

> 🔑 **我拿一個時戳去回答它沒有在回答的問題。**

📌 這與今天的 [[verify-against-known-good-output]] 同族，但更隱蔽：
**欄位名稱看起來像答案**（`pushedAt` ↔「什麼時候上去的」），
**而它答的是另一個事件**。前面幾次是「讀了旁邊那一欄」，這次是**讀對了欄、問錯了事件**。

⚠️ **「公開了三個多小時」這句我在兩封訊息裡都寫過，兩封都要更正。**
**正確的說法**：「**已經公開，時間點不明**」——而**那不影響任何結論**（授權完整、內容已被逐項裁決）。

### ③ 規模這件事**沒有被放到 Adam 面前**（`開機手冊` 自己指出的）

他給 Adam 的是**三個類別**，不是「**`doc/audit/` 佔公開 repo 的 56%**」（563 / 999）。
⇒ 他會**補正**給 Adam，**但不重新問**——同一件事他已經裁決兩次。**我同意這個處理。**

⇒ **公開這條線結案。** 我不再持有任何未處理的疑慮。

---
