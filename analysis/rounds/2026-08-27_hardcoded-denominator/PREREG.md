# 工單 Q 預註冊：修掉寫死的分母

**寫於 2026-08-27 下午，`8/27 mainDev`。實驗室不在我手上、P 還沒跑、Q 一行碼都還沒改。**
append-only：P 的結果出來後若要調整，用增補，不改本文。

---

## Q-0. 標的與機制（讀碼，不是推測）

`src/ndt_core/collection/FlowLinkUsageCollector.cpp:1947-1949`：

```cpp
m_topologyAndFlowMonitor->updateLinkInfoLeftLinkBandwidth(
    agentKeyOtherSideOpt.value(),
    counter.inputByteCountOnALinkMultiplySampingRate * 8);   // bytes×8 → 直接當 bps
value.inputByteCountOnALinkMultiplySampingRate = 0;          // 累加器每輪清空
```

累加器每輪清空、乘 8 就當 bps 送出，**路徑上沒有任何一處除以實際經過時間**。
只有「一輪剛好一秒」時才正確——而 `:1725` 的註解自己就說了它不可能：
**`sleep_for(1s)` 在本體之前，所以週期＝1 s ＋ 本體耗時。**

實測（工單①，n=3、rep 間 SD 3.4 ms）：**安靜 1043.9 ms**、64 流 ~1255 ms。

## Q-1. 🔴 **我要先反對審查員給的一條閘門** —— 一個正確的修法會被它擋下來

他的判讀表寫著：

> **週期行沒讀 ~1000 ⇒ 改錯地方了，不看其他數字**

**這條在兩種修法之下意義完全不同，而其中一種會讓正確的修法被判失敗**：

| 修法 | 做什麼 | 修完週期行讀什麼 | 閘門結果 |
|---|---|---|---|
| **(A) 除以實測經過時間** | `bytes*8 / elapsed_s` | **仍是 1043 ms**（迴圈沒變快） | 🔴 **被閘門擋下——儘管它是對的** |
| **(B) `sleep_until(next+1s)`** | 讓週期真的等於 1 s，假設就成真 | **~1000 ms** | ✅ 通過 |

⇒ **(A) 是正確但會被誤判的那個；(B) 是通過閘門但脆弱的那個**——
(B) 一旦本體耗時超過 1 s（重負載下完全可能，64 流的本體已經 ~255 ms），
週期又會溢出，**而那時候 bug 會無聲地回來，因為程式碼裡再也沒有除法保護它**。

**⇒ 本輪採 (A)＋(B) 兩者都做**，並且**閘門照他寫的保留**：
- **(B)** 讓穩態週期真的是 ~1000 ms ⇒ 他的閘門有效、可以照用；
- **(A)** 讓「本體超過 1 s」時仍然正確 ⇒ 脆弱性被移除。

📌 **這一段是本預註冊最重要的部分**：**一個正確的修法會失敗的閘門，比沒有閘門更糟。**
我在動手之前講，是因為事後講會變成「我沒過閘門所以我要改閘門」。

## Q-2. 前置條件（今天之前不成立）

報告 §8 的 `1-quater` 寫著「改碼，**在 1-bis 有數字之後**」。
1-bis ＝量出迴圈週期 ＝ **工單①已完成**（1043.9 ms、n=3、SD 3.4）。⇒ 前置滿足。

## Q-3. 驗收儀器（**單獨成節，因為它是唯一不需要 ratio 就能否掉整個交付的判準**）

`:1725-1742` 前人留下的儀器（3 個 `periodSumMs`，每 30 輪 INFO 一次）。原註解：

> *It is also the acceptance instrument for the fix. Once the accumulator is divided by the
> measured interval instead of an assumed one, this line must read ~1000 ms forever.*

**判準（在 Q-1 的 (A)+(B) 修法之下有效）**：修完之後，**穩態週期行必須讀 1000 ± 20 ms**。

🔴 **沒過就是改錯地方了，不看其他數字。**（採納審查員原文。）

⚠️ **但要注意這個儀器現在不在磁碟上的 binary 裡**：HEAD 的原始碼有它，
`build/bin/ndtwin_kernel` 是**無儀器的 `3367d0e9`**。
⇒ **Q 兩臂都必須重建**，而重建會永久換掉 `3367d0e9`。
**動之前通知 `8/27 sampling`**，並在報告裡記兩臂各自的 sha256。

🔴 **before 臂不得用磁碟上的 `3367d0e9`**——它與 HEAD 除了本次改動之外**還差一組儀器**，
拿它當 before ＝一次動兩個變數。**before 必須是 HEAD 的新鮮重建。**

## Q-4. 預測（**區間，寫死在資料之前**；審查員給的，我照收）

| | 現值 | `T/1000` | 預測修完後 |
|---|---|---|---|
| **16 流** | 1.037 | 1.044 | **0.97–1.01** |
| **64 流** | 1.193 | 1.255 | **0.92–0.98** |
| **週期行** | 1043.9 ms | — | **1000 ± 20 ms** |

🔴 **若修完 ratio 反而更偏離 1** ⇒ 分母不是主因，**①的「同輪同視窗 `T` 解釋掉幾乎全部高報」要重審。**

## Q-5. 判讀表（先寫死，不得事後調整）

| 結果 | 裁決 |
|---|---|
| 兩個流數都落區間、週期行 1000±20 | **修好了**；殘差交給 P 解釋 |
| 落在 1.0 附近但殘差 > P 能解釋的量 | 修好了，但**還有第三個機制**，開新題 |
| 更偏離 1 | 分母不是主因，**回頭重審①的歸因** |
| 週期行沒讀 1000±20 | **改錯地方了**，不看其他數字 |
| **以上皆非** | **記成獨立結果，不塞進最近的分支**（援工單 E 的 29.49 先例） |

## Q-6. 🔴 殘差的解釋：**待 P 填入的空格，不先猜值**

審查員的算術：64 流實測 **1.193** 對 `T/1000` **1.255**，中間差約 **5%**，
候選解釋是「樣本已經在掉」＝ P 要指認的那個。**只修分母的話那一格會從高報 19% 變成低報約 5%**
——絕對誤差變小但**符號翻過去**。

⚠️ **他自己標明了「這段是算術吻合，不是機制」，我同意並照這個層級對待它。**

⇒ **本節留空**：

> **殘差 ＝ ______（待工單 P 的結論填入）**
> P 未出結果之前，Q 的報告**不得**對殘差給出解釋，也**不得**先填一個猜測值再回頭改。

## Q-7. 臂與可比性（沿用①學到的）

- **兩臂同一代 fabric、同一顆 kernel process 不可能**（換 binary 必須重啟）⇒
  **同代 fabric、只換 binary**，照增補 D 臂 U 的做法（`restart_kernel.sh`，sha256 斷言）。
- **兩臂之間 NTG 拓撲檔狀態必須一致**——`ndtwin-lab` 起 OVS 跑的是
  `~/Network-Traffic-Generator/testbed_topo.py` **不是 repo 那份**，而 N 會改它再還原。
  **跟 `8/27 sampling` 對時間表，before/after 不得跨越還原邊界。**
- **兩臂在同一個 VM 狀態下跑**——那台 4 vCPU QEMU 起於 **08-27 12:41:45**，
  跨越該時刻的兩臂不可比。
- **負載用 `/proc/stat` 差分量化，不用 `loadavg`**（①的中止條件用閒置讀數校準卻在量測中判定，第一臂就違反）。

## Q-8. 交付

一處改動（掛 attribution）／修前修後同 fabric 配對量測（16 與 64 流各一）／週期行讀數／
兩臂 sha256／**對帳節：明列這推翻或更新了哪些既有結論，特別是所有引用過 ratio 的頁**。

🔴 **改動要有一個會失敗的測試守著——沒看過它失敗就不算交付。**
（分母的測試形狀：餵已知位元組數與已知經過時間，斷言算出的 bps；
突變＝把除法拿掉，測試必須紅。）

## Q-9. 對本輪自己的紀律（沿用 P-8）

**哨兵（`|Q′−Q| > |B14−Q|/2 ⇒ CONFOUNDED`）與判讀表，要對報告裡的每一個宣稱各跑一遍，
不是只對掛著「裁決」標籤的那一個跑。** ①最後那個錯誤就是這樣發生的：
我寫下了削弱自己的事實，卻沒讓它回頭改掉結論句。**寫在這裡是為了讓驗收方能拿它退我的件。**

## Q-10. 本節**不**宣稱

不宣稱天花板（08-20 已永久撤回外推）；
不宣稱這修好了大規模高報那個頭條（**那要 P 的結論一起才完整**）；
不宣稱 OVS 平面同樣受益（未測）；
不宣稱殘差的成因（見 Q-6，留空）。

[Co-developed with claude code -- Adam]

---

# 增補 Q-bis（append-only，2026-08-27 下午，仍未改任何一行碼）

審查員接受 Q-1 的反對並自己讀碼確認：`:1753-1755` 的 `periodMs` 量在 `sleep_for(1s)` **之後**
⇒ 週期＝1000 ms ＋ 前一輪本體 ⇒ **修法 (A) 不動睡眠與本體，週期仍讀 1043 ms、會被原閘門誤殺。**

## Qb-1. 🔴 原閘門作廢，而**錯的源頭在原始碼註解裡**

`FlowLinkUsageCollector.cpp:1736-1737` 寫著：

> *Once the accumulator is divided by the measured interval instead of an assumed one,
> **this line must read ~1000 ms forever**.*

**那句話不成立**，理由同上。審查員是**從這段註解抄成工單條款的，沒有自己推一遍**。

🔴 **交付要順手修掉那句註解**（掛 attribution、寫明它為什麼錯）。
理由：那是 `1-quater` 的執行者會讀的第一段文字，**它已經騙過一個人了**。
⇒ 同 [[cited-line-numbers-are-not-evidence]]：**註解裡的宣稱也是宣稱。**

## Qb-2. 新驗收儀器（取代 Q-3 與判讀表第四列）

> **記錄「實際被當成除數用的那個值」，並斷言它等於同一輪量到的經過時間（容差 1%）。
> 兩者不符 ⇒ 改錯地方，不看其他數字。**

檢查的是**自我一致性**而非某個特定數值 ⇒ **對 (A)、(B)、(A)+(B) 三種修法都有效**。
採 (B) 時**額外**加：穩態週期須讀 **1000 ± 20 ms**（那是 (B) 自己的驗收，不是 (A) 的）。

## Qb-3. 🔴 (B) 的追趕守衛（審查員要求，理由是這個 repo 的前科）

`sleep_until(next += 1s)`：**若本體超過 1 s，`next` 落在過去 ⇒ 迴圈不睡、連續空轉追趕 ⇒ CPU 燒滿**
——本 repo 已經有過一個 idle 100% CPU 的 bug（`poll()` 用 0 ms timeout）。

**寫法定死**：`next` 落後於 `now` 時**直接跳到下一個未來刻度，不累積補償**；
**跳過時記一筆** —— 那本身就是「本體超時」的訊號，對 P 與 Q 都有用。

[Co-developed with claude code -- Adam]

---

# 增補 Q-ter（append-only，2026-08-27 16:1x，**仍未改任何一行碼**）

## Qt-1. 🔴 我的 Q-0 只點名了一處，**實際上有兩處**

`grep -n "MultiplySampingRate" FlowLinkUsageCollector.cpp`：

| 行 | 函式 | 服務的邊 |
|---|---|---|
| **`:1697`** | `creditHostBoundEgressEdges()` | **host-bound egress 邊**（`findEdgeToHostByAgentIpAndPort`） |
| **`:1949`** | `calAvgFlowSendingRatesPeriodically()` | 主迴圈 |

**兩處都是 `累加器 * 8` 直接送進 `updateLinkInfoLeftLinkBandwidth`，兩處都每輪清零。**
`:1697` 自己的註解就寫著 *"an entry must not carry bytes into the **next second**"*
——**同一個「一輪等於一秒」的假設，同一個缺陷。**

⇒ **兩處都要修。** 我的 Q-0 寫「標的：`:1947-1949`」是**不完整的**，此增補補上。

### 為什麼這件事在動手前發現很重要

`:1697` 服務的是 **host-bound 邊**，而工單①量的三個類別裡有兩個
（`host→switch`、`switch→host`）就是它們。
⇒ **只修 `:1949` 會產生「部分邊類修好、部分沒修」的結果**，
而那在 Q-5 的判讀表裡會長得像**「更偏離 1」**（⇒ 誤判為「分母不是主因，回頭重審①」）
或**「還有第三個機制」**——**兩個都是錯的裁決，而且都會把後續工作導向不存在的問題。**

📌 這是「[[existence-is-not-wiring]] 的反面」：不是找到呼叫點就以為找到定義，
而是**找到一處就以為那是全部**。**`grep` 要數完，不要 `| head`。**

## Qt-2. 對交付的影響

- Q-8 的「一處改動」改為 **兩處改動**（同一個 helper，兩個呼叫點共用）。
- 測試要**同時覆蓋兩條路徑**：主迴圈與 host-bound egress。
- **驗收儀器（Qb-2：除數 == 同輪經過時間）要對兩處各斷言一次**，
  否則「其中一處沒改到」正好是它偵測不到的那個缺口。

[Co-developed with claude code -- Adam]

---

# 增補 Q-quater（append-only，2026-08-27 17:0x，**仍未改任何一行碼**）

## Qq-1. 🔴 **Q 的工作點必須帶負載** —— 安靜 fabric 上這個缺陷會自己消失

**來源：我自己一則宣稱的撤回。** 我在 P 開跑前向審查員報過一則「免費對帳」：
新 P4 世代的煙霧臂量到 `T` = 1044 ms，與①安靜臂的 1043.9 ms 逐位吻合 ⇒ 迴圈週期跨重建可重現。

**那則對帳不成立，而且是被同一輪二十分鐘後的更好資料反駁的**（見
`../2026-08-27_telemetry-blindness/compare_T_across_generations.py`）：

| 對（同 burner 數） | gen 1（①） | gen 2（P） | Δ | **Δ/SE** |
|---|---:|---:|---:|---:|
| **B14** | 1071.1 ± 15.9 | **1074.0** ± 21.5 | −2.9 ms | **0.2** ✅ 複製 |
| **Q（安靜）** | **1043.9** ± 3.4 | **1001.7** ± 5.8 | **+42.2 ms** | **10.9** 🔴 不複製 |

煙霧臂是 `T_REPS=1`、`FLOW_S=30`（①是 3 與 340），且我引用的是被 `tail -8`
留下的**衍生行** `predicted over-report: 1.044x`，而**帶不確定性的 `mean X s over N gaps` 那一行剛好被切掉**。
⇒ 那個「吻合」是整組裡最不可靠的一次量測，落在正中央。

## Qq-2. 因此 Q-4 的預測區間**校準基準失效**

Q-4 的區間是拿①的 **1043.9 ms**（⇒ 分母造成 **+4.4%** 高報）校準的。用今天的數字：

| 條件 | `T` | 寫死 1000 ms 造成的高報 | 對 Q 的意義 |
|---|---:|---:|---|
| **安靜（gen 2 實測）** | 1001.7 | **+0.2%** | 🔴 **落在噪聲裡，Q 量不到東西** |
| **14 burners（實測且跨世代複製）** | 1074.0 | **+7.4%** | ✅ 效應大於噪聲，且**基準本身是可複製的** |

🔑 **這與審查員替工單 M 找到的排序陷阱是同一族：改動使被測效應自己消失。**
差別在於 M 的陷阱要等 M 跑完才發動，**Q 的已經發動了** —— 若照原計劃在安靜 fabric 上量，
我們會量到 0.2%、判成「分母無害」，**而那是錯的結論。**

## Qq-3. 因此本輪修訂（**修訂在資料之前，不是事後調整**）

1. **工作點改為 14 burners**，不是安靜 fabric。理由：那一格的 `T` 是目前**唯一跨世代複製過**的
   （Δ/SE = 0.2），既有效應又有已驗證的基準。
2. **Q-4 的預測區間改用 gen-2 的 `T`**，且**區間寬度必須含儀器散佈**（±21.5 ms ⇒ ±2.1 個百分點），
   不得用①的 1043.9 當點估計。
3. **保留一個安靜臂**做對照，並**預先寫下它的預期**：安靜臂的修前/修後差應 **≈ 0**（0.2% 量級）。
   ⚠️ **這一臂若也出現大的改善，代表機制不是分母** —— 這是本輪唯一能否掉自己主假說的觀察。
4. **每個臂必須在同視窗內量 `T`**（`T_REPS=3`），不得沿用別輪的 `T`。跨世代沿用正是本增補在修的錯誤。

## Qq-4. 🔴 本增補**不**宣稱

- **不**宣稱安靜臂的 `T` 為什麼從 1043.9（11:59）→ 1016.7（①的 Q′，12:26）→ 1001.7（16:41）單調下降。
  三個點，**沒有機制，不寫**。
- **不**宣稱 `T` 隨負載上升 —— 那正是①判 CONFOUNDED 的宣稱，**仍然是撤回狀態**。
  本增補只用「B14 那一格跨世代複製」這個**成對比較**，不用跨負載的縱向比較。
- **不**宣稱 gen-1 的 1043.9 是錯的。兩代都是實測，**差異本身才是未解釋的東西**。

📌 順帶更正一則自己的讀法：①的 Q/Q′ 差 **27.2 ms** 當時被我讀成「該量不可解讀」。
**它其實還說了第二件事：安靜臂的 `T` 這一格本身在動。** 我只讀了前半。

[Co-developed with claude code -- Adam]

---

# 增補 Q-quinquies（append-only，2026-08-27 18:0x，**仍未改任何一行碼**）

## Qq5-1. 🔴 **Q-ter 的邊類對應是錯的**（讀碼更正，來源：工單 P §5-bis）

Q-ter 寫：

> `:1697` 服務的是 **host-bound 邊**，而工單①量的三個類別裡有兩個
> （`host→switch`、`switch→host`）就是它們。

**`host→switch` 不是 `:1697` 服務的。** `FlowLinkUsageCollector.cpp:1444-1461` 把**同一個樣本**
記進兩張表（一張按 ingress port、一張按 egress port），然後：

| 站點 | 付出的表 | **服務的邊類** |
|---|---|---|
| **`:1949`** 主迴圈 | `m_counterReports`（按 ingress） | **`host→switch` ＋ `switch→switch`** |
| **`:1697`** `creditHostBoundEgressEdges` | `m_egressCounterReports`（按 egress，只付 host-bound） | **只有 `switch→host`** |

`:1697` 的註解自己寫著：*"a switch far end means the downstream sampler owns the edge"*
⇒ switch→switch 的 egress 側被**丟棄不寫**，由下游的 ingress 側負責。

## Qq5-2. 對 Q 的影響：**結論不變，預期換位**

- ✅ **「兩處都要修」不變。** 兩處都是 `累加器 * 8` 送進 `updateLinkInfoLeftLinkBandwidth`，同一個缺陷。
- 🔴 **「修了哪一處 ⇒ 哪一類會動」的對應要換：**

| 若只修 | 會動的邊類 | 不會動的 |
|---|---|---|
| `:1949` | host→switch、switch→switch | switch→host |
| `:1697` | switch→host | 另外兩類 |

⇒ **驗收要對三類分別預期**，不能只看總體。Q-ter 警告過的誤判（讀成「更偏離 1」或「有第三個機制」）
**在錯誤的對應下仍然會發生，只是換一個方向。**

## Qq5-3. 🔴 **`host→switch` 那一類的 ratio 不適合當 Q 的驗收指標**

工單 P §5-bis：`host→switch` 的**分子**是樣本（只有進到 P4 管線的封包才被取樣），
**分母**是 veth 的 **RX**（Linux 在介面上數的，**含 bmv2 來不及讀走的**）。
⇒ **那一欄混進了一個與分母無關的效應**（bmv2 ingest 存活率），而且它在負載下很大（B28 掉到 0.65）。

⇒ **Q 的驗收優先用 `switch→switch`（`:1949`）與 `switch→host`（`:1697`）**，
兩者的分母都只含「已通過上游」的位元組。**`host→switch` 照記但不當判準**，
直到 P §8 的 veth pair 對帳做完。

⚠️ **這與 Q-quater 的工作點決定沒有衝突**：14 burners 仍是 `T` 唯一跨世代複製的那一格。
但**要注意 14 burners 也會放大 bmv2 的 ingest 丟包** ⇒ 更加不能用 `host→switch` 當判準。

[Co-developed with claude code -- Adam]

---

# 增補 Q-sexies（append-only，2026-08-27 17:5x，**仍未改任何一行碼**）

## Qs-1. 🔴 **Q-quater 的推理作廢**（工作點的**結論**待裁，見 Qs-3）

Q-quater 說：安靜臂 `T` = 1001.7 ⇒ 分母只高報 0.2% ⇒ Q 量不到東西 ⇒ 改用 14 burners。

**那個 1001.7 是單一安靜臂。** 第四臂 `P_Qp` 進來是 **1043.3** ⇒
**同一代的兩個安靜臂差 41.6 ms**（gen 1 的兩個差 27.2 ms）。

⇒ **安靜臂的真實值是 ~1022 ± 25（臂間），高報 +2.2%，不是 +0.2%。**
⇒ 而 Q-quater 據以宣稱「跨世代不複製」的 **Δ/SE = 10.9 也是錯的**：
分母用了**臂內 rep 的散佈**，分子是**臂間差異**。用臂當複製單位重算 ⇒ **Δ/SE = 0.31，沒有差異。**
見 [[replication-unit-not-the-rep]]。

## Qs-2. 工作點普查（審查員提案；同一代 fabric、同一顆出貨 binary `3367d0e9`）

| 候選 | `T` | 分母造成的高報 | 來源 |
|---|---:|---:|---|
| 16 流安靜 | **~1022 ± 25**（臂間） | **+2.2%** | `P_Q` ＋ `P_Qp` |
| **64 流安靜** | **1047.3 ± 14.3** | **+4.7%** | `Q_T64`（1060.9 / 1048.6 / 1032.4） |
| 16 流 + 14 burners | **1074.0 ± 21.5** | **+7.4%** | `P_B14` |

**64 流沒有贏。** 而且 🔴 **三者全距只有 52 ms，而安靜臂的臂間散佈就有 42 ms**
⇒ **Q 想量的效應 5–7%、臂間噪聲約 4%。每一個工作點的訊噪比都差。**

## Qs-3. 🔴 **不要用這三個數字選工作點——它們都來自一個在關鍵區間未驗證的估計量**

gen 1 用**迴圈內儀器**量 64 流 = **1248.7 ± 30.8**（自 `p4_T64/kernel.log` 的累計平均差分還原）。
今天用**外部估計量**量到 **1047.3**。**差 200 ms。**

- 兩法**只在 16 流交叉驗證過**（1041.2 對 1043.9，差 2.7 ms）；**64 流從未比過**。
- 🔴 **估計量可能的失效方向正好吻合**：迴圈本體越慢 ⇒ 一次迭代的寫入散得越開 ⇒
  一個 cluster 可能被拆成兩個 ⇒ **估計值偏低**。**不能排除今天的 1047 是低估。**

### ✅ 解法不花額外成本

```
strings build/bin/ndtwin_kernel | grep -c "rate loop period over"  → 0
grep -c "rate loop period over" .test_run/logs/kernel.log          → 0
```
出貨的 `3367d0e9` **不含儀器**，但**原始碼含**（`FlowLinkUsageCollector.cpp:~1781`），
而①的 §1-nonies 自己就寫著「**同一行就是修法的驗收儀器**」。
**Q 是改碼工單，本來就要重建一次。**

⇒ **本增補註冊的順序：重建（帶儀器）→ 普查（迴圈內儀器）→ 註冊工作點 → 才跑 Q 的臂。**
**工作點在有 ground truth 之前不註冊。**

## Qs-4. 待裁：**若普查顯示每個工作點都只有 5–7% 而臂間噪聲 4%**

| | 做法 | 代價 |
|---|---|---|
| (a) | **每格 3 臂**，把臂間散佈壓成 SE | 三倍機時 |
| **(b)** | **改用迴圈內儀器當主要判準**，不走 ratio | 見下方 ⚠️ |
| (c) | 照原計劃跑，接受可能判不出來 | 可能無結論 |

**建議 (b)**：①的 §1-nonies 已證明那行儀器**空載讀 1000.1 ms（min 1000.1 / max 1000.2）**，
**精度 0.1 ms，比效應小 50 倍**；而 ratio 那條路要穿過本輪剛發現的一堆東西
（邊類量測點不同、bmv2 ingest 丟包、臂間 42 ms）。

⚠️ **(b) 的界線要寫死**：它驗的是**「分母被修對了」**，
**不是**「高報消失了」。**後者仍然需要 ratio。兩個是不同的宣稱，(b) 不得冒充後者。**

[Co-developed with claude code -- Adam]
