# 初版簡報（v1）生成說明 — 2026-08-13

產出：`NDTwin-progress-report-draft-v1.pptx`（38 頁，含全部講者備註）。
來源：`NDTwin-slide-template.md`（08-13 校訂版）為唯一權威；`S-slide-review.md` 只當背景——
它的建議大多已被校訂版吸收，其「readopt 不要上台」已被後來的修復（`a72a168`）與
「readopt 進穩健性頁」的裁決取代。

## 依 A3.1 動筆當下重量的數字（head `cfbbf24`）

| 項目 | 值 | 備註 |
|---|---|---|
| commits `28b8b13..HEAD` | **296** | 全部 Adam010341（單一作者已驗） |
| gtest | **554** cases / **44** .cpp | `TEST_P`=0 已驗，grep 數＝runtime 數；與實跑 554/70 suites 一致 |
| Python | **402**（p4_proxy/tests，15 檔）＋ **232**（tests/python，7 檔）＝ 634 | |
| shell | **6** 支 | |

## 落筆時做的裁定（初版採用，請過目）

1. **投影片 22 的 ④（TESTBED 電源）**：template 原 ④ 與 `3292653` message 的矛盾已用
   baseline 原文解開——**兩句都對，指不同 overload**。`28b8b13` 的 `:368` 三參數版
   會照 caller 要求改 graph（scrape 出的狀態只寫 log），但**零呼叫點**；`:689→:716`
   可達版是裸 curl `rc==0`、從不碰 graph。初版把兩份實作合寫成一條完整故事
   （「同一個操作、兩份實作，兩份都說謊」）。A2 原本因矛盾把 `3292653` 列排除——此寫法待你確認。
2. **投影片 20 的 ⑤（OVS 路由 stale rule）**：`cfbbf24` 才查證坐實、template 校訂沒趕上。
   照 `2c81b26`「既有 Ryu 控制程式」先例列入「只會加、不會刪」家族，標「已查證、刻意未修、
   處置待裁決」。要不要上台、三個處置選項（狀態檔 §3）都等你裁。
3. **投影片 16 的「11 個」**：保留為第一輪驗證的數字，追加條目用「其後的獨立審查又坐實多條」
   帶過——S 報告問過要不要改總數，你沒裁，初版不改。
4. **投影片 28 的「11+」**（mutation 抓到的無效測試數）：CHANGELOG 記 11（08-08 時點），
   其後輪次未逐條重數，維持下限表述。S 建議的方法論第 ⑤ 點「引用會腐爛」未加（校訂版沒採）。

## 已照既有裁決執行

- readopt 進穩健性頁（投影片 14）、單向斷鏈進 bug 頁（投影片 23，獨立一頁）。
- `1a7d815` 拆半：kernel 半邊在投影片 24 ④、proxy 半邊在投影片 14——兩頁都明寫「同一事件的兩半」。
- `916d330` 拆半：power 半邊在投影片 22 ⑥、OpResult 半邊在功能頁（投影片 15）。
- 55 條審查輪不開專頁、來源不提；其 baseline 條目散進第 2 節（`f21d7a0`、`57346f6`、
  `cbb504a`、`5054249`、`59dc5d3`、`ee7233b`——最後一條當投影片 16 的開場引例）。
- 全程無 AI 協作字樣（A1.3）；`57346f6` 標明是產品自己的 Intent Translator。
- 第 5 節骨架預填：投影片 33 的 failover 數據表已用實測數字填滿，其餘留
  【Adam 後續提供】（32/34/35/36/37 共 6 處＋標題頁指導教授姓名）。

## 品質檢核與已知限制

- OOXML 驗證：PASS。幾何檢查（越界／溢出估算／文字框互疊）：乾淨——初跑抓到 3 個
  版面問題（架構頁標籤壓標題、兩頁 caption 被 bullets 框蓋住），已修。
- ⚠️ **這台機器沒有 LibreOffice，做不了像素級渲染 QA**。字型用微軟正黑體，溢出是用
  CJK 字寬估算的——**開啟後請人工快速過一輪版面**，尤其投影片 5（架構圖）、22、24（字最多）。
- 講者備註每頁都有：素材出處（commit／檔案）、template 頁碼對應、與上述待決旗標。
